import {SafeAreaView, StyleSheet, Text} from 'react-native';

const App = () => {
return (
    <SafeAreaView>
    <Text>Hi</Text>
</SafeAreaView>
);
}

const styles = StyleSheet.create({
});

export default App;
